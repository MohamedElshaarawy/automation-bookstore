# HTML
# Archive your static HTML project and save it with the build record.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: ubuntu-latest

stages:

# =========================
# 1️⃣ BUILD STAGE
# =========================
- stage: Build
  displayName: Build HTML
  jobs:
  - job: BuildJob
    steps:
    - task: ArchiveFiles@2
      displayName: 'Archive HTML'
      inputs:
        rootFolderOrFile: '$(Build.SourcesDirectory)'
        includeRootFolder: false

    - task: PublishBuildArtifacts@1
      displayName: 'Publish Artifacts'

# =========================
# 2️⃣ POSTMAN TESTS (BEFORE DEPLOY ✅)
# =========================
- stage: Postman_API_Tests
  displayName: Run Postman API Tests
  dependsOn: Build
  condition: succeeded()

  jobs:
  - job: RunPostman
    displayName: Execute Postman Collection
    pool:
      vmImage: ubuntu-latest

    steps:
    - task: NodeTool@0
      displayName: Install Node.js
      inputs:
        versionSpec: '18.x'
        targetType: 'inline'

    - script: |
        npm install -g newman newman-reporter-htmlextra
        echo "Newman version:"
        newman --version

        echo "Running Postman collection..."
        newman run postman/QC-Website.postman_collection.json \
        --reporters cli,junit,htmlextra \
        --reporter-junit-export newman-results.xml \
        --reporter-htmlextra-export newman-report.html \
        --reporter-htmlextra-title "QC Website API Tests" \
        --reporter-htmlextra-darkTheme
      displayName: Run Postman API Tests


    - task: PublishTestResults@2
      displayName: Publish Postman Results
      condition: always()
      inputs:
        testResultsFormat: JUnit
        testResultsFiles: newman-report.xml
        failTaskOnFailedTests: true

        
    # Publish HTML report as artifact
    - task: PublishPipelineArtifact@1
      displayName: Publish Newman HTML Report
      condition: always()
      inputs:
        targetPath: 'newman-report'
        artifactName: 'Postman-HTML-Report'


